{
  "name": "dafreeai-studio-cf",
  "version": "1.0.0",
  "private": true,
  "description": "DaFreeAi Studio — Cloudflare Pages + Functions (Phase 1 MVP)",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2024-11-01",
    "deploy": "wrangler pages deploy public --project-name=dafreeai-studio",
    "tail": "wrangler pages deployment tail --project-name=dafreeai-studio"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
