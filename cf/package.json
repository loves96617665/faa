{
  "name": "dafreeai-studio-cf",
  "version": "1.0.0",
  "private": true,
  "description": "DaFreeAi Studio — Cloudflare Worker + Assets (Phase 1 MVP)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
